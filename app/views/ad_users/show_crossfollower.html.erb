<p class="fsb">フォロワー</p>
<% @crossfollows.each do |crossfollow| %>
<table>
<tr>
  <td>  ニックネーム：　<%= StUser.find(crossfollow.followable_id).nickname %>&nbsp;&nbsp; </td>
  <td><%= link_to '詳細', st_user_path(StUser.find(crossfollow.followable_id)) %></td>
</tr>

</table>
<% end %>