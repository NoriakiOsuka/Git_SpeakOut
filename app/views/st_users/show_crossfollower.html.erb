<% @crossfollows.each do |crossfollow| %>
<table>
<tr>
  <td>  <%= AdUser.find(crossfollow.followable_id).nickname %> </td>
  <td><%= link_to '詳細', ad_user_path(AdUser.find(crossfollow.followable_id)) %></td>
</tr>

</table>
<% end %>